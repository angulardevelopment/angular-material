import { TitleCheckerDirective } from './title-checker.directive';

describe('TitleCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
